[lint]
ignore = [
    "E402",   # Module import not at top (required for sys.path manipulation)
    "F401",   # Unused imports in try/except availability checks
    "C901",   # Function too complex
]

[lint.per-file-ignores]
"scripts/*" = ["E402", "F401"]
"tests/*" = ["F841"]
